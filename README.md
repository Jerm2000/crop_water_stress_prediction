# crop_water_stress_prediction
Must include: 
1. Description - A description of what the trained model is and does (include details on the model
structure used such as any engineered features and/or correlation structures)(That means that we can write out the model). In this description, include a measure of how well your model predicts. On a grading note, I have tried several models
for this project so I know what a good model prediction accuracy is. If you using a model that gets
significantly higher than the RMSE of my best model, you will be docked accordingly.
2. Dependencies - A list of any library dependencies that are needed to appropriately use the model
3. Object(s) - A list of the object(s), names and their description including class.
4. Usage - a snippet of code that shows how to use your saved object(s). In this case, you should
show code on how use the objects in your rds or RData file to generate the appropriate
predictions for the whole field and create a corresponding ggplot() . Note that this snippet should
NOT do any model training but rather simply use a pre-trained model. In our grading, we will use
this snippet exactly as written. If this code throws an error or doesnʼt work appropriately, the max
grade you can get is 50%.
5. Output - description of any output that comes from your usage code above


Description:

Dependencies:

Objects:

Usage:

Output:
